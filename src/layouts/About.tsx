import { Content, Panel } from "rsuite";

export function About() {
    return (
        <Panel header={<h3>About</h3>}>
            <Content>
                Forums JS is a forums app written in JS.
            </Content>
        </Panel>
    );
}
